export default {
    holdtype: {
        AUTO: 0,
        PAUSED: 1,
        MANUAL: 2,
        OFF: 7,
        /*getLabel(value) {
            for (const [key, val] of Object.entries(this)) {
                if (key !== 'getLabel' && val === value) {
                    return key;
                }
            }
            return null;
        }*/
    }
}